services:
  api:
    build: ./api
    ports:
      - "0.0.0.0:5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      - API_KEY=change-me-in-production
      - DB_PATH=data/cards.db
    restart: unless-stopped